# Convex codegen output is committed on purpose: the web/admin apps import
# `@convex/_generated/api`, and CI has no Convex deploy key to regenerate it.
# Regenerate locally with `convex dev` / `convex codegen` and commit the diff.
.env.local

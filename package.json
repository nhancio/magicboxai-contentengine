{
  "name": "magicboxai",
  "private": true,
  "scripts": {
    "build": "npm run build:hosting && npm run build --workspace=functions",
    "build:landing": "npm run build --workspace=magicbox-landing",
    "build:web": "npm run build --workspace=magicbox-web",
    "build:admin": "npm run build --workspace=magicbox-admin",
    "build:hosting": "npm run build:landing && npm run build:web && npm run build:admin",
    "typecheck": "npx tsc -p apps/landing/tsconfig.json --noEmit && npx tsc -p apps/web/tsconfig.json --noEmit && npx tsc -p apps/admin/tsconfig.json --noEmit && npx tsc -p functions/tsconfig.json --noEmit",
    "test": "npm test --workspace=functions",
    "test:e2e": "python3 tests/e2e/smoke.py"
  },
  "workspaces": [
    "apps/*",
    "functions",
    "packages/*"
  ],
  "allowScripts": {
    "@firebase/util": true,
    "esbuild": true,
    "@swc/core": true,
    "fsevents": true,
    "protobufjs": true
  },
  "overrides": {
    "fast-uri": "3.1.4",
    "fast-xml-parser": "5.10.1",
    "websocket-driver": "0.7.5"
  }
}
